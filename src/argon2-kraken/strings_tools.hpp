#ifndef STRINGS_TOOLS_H
#define STRINGS_TOOLS_H

#include <string>

std::string hexToString(const std::string hex_string);
std::string stringToHex(const std::string& input);

#endif // STRINGS_TOOLS_H
